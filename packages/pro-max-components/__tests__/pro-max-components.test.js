'use strict';

const proMaxComponents = require('..');
const assert = require('assert').strict;

assert.strictEqual(proMaxComponents(), 'Hello from proMaxComponents');
console.info('proMaxComponents tests passed');
